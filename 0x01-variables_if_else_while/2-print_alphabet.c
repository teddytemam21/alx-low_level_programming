#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry point
 *
 * Return: always 0 (success)
 */
int main(void)
{
       	char ch;

    	for (ch = 'a'; ch <= 'z' ; ch++)

		        putchar(ch);
		        printf("\n");

		        return (0);
}
